alert('Este archivo sera borrado para probar el comando reset!')

// borre el archivo de la papelera, e hice el reset hard. Todo esta recuperado.
// esta linea esta siendo agregada para usar el comando amend

const a = 1;
const b = 2;
const c = 3;

const suma = (a + b + c)
console.log(suma);